<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_conn"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:focusable="true"
    android:focusableInTouchMode="true"
    tools:context="com.example.yuesh.sshmaster.Conn">


    <TextView
        android:text="请先点击连接"
        android:layout_width="match_parent"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:singleLine="false"
        android:scrollbars="vertical"
        android:id="@+id/tee_textview"
        android:layout_height="350dp" />

    <Button
        android:text="连接"
        android:layout_width="100dp"
        android:layout_height="50dp"
        android:id="@+id/tee_button"
        android:onClick="connect"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true" />

    <EditText
        android:layout_height="50dp"
        android:inputType="textPersonName"
        android:text="此处输入命令"
        android:ems="10"
        android:id="@+id/tee_editText"
        android:layout_width="220dp"
        android:layout_alignTop="@+id/tee_send"
        android:layout_alignParentStart="true" />

    <Button
        android:text="发送"
        android:layout_width="100dp"
        android:layout_height="50dp"
        android:id="@+id/tee_send"
        android:onClick="send"
        android:layout_marginTop="16dp"
        android:layout_below="@+id/tee_textview"
        android:layout_alignParentEnd="true" />
</RelativeLayout>
